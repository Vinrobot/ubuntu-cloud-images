{
  "workspaces": [
    "packages/ubuntu-cloud-images",
    "packages/vmware-content-library",
    "packages/scraper"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.4",
    "ts-node": "^10.9.1",
    "tslib": "^2.5.2",
    "typescript": "^4.9.5"
  }
}
